[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  included_files = ["server/**", "assets/email/**"]
  external_node_modules = ["nodemailer", "@netlify/blobs"]

[[redirects]]
  from = "/api/ssot"
  to = "/.netlify/functions/ssot"
  status = 200
